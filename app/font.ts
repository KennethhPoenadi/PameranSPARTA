import { Quicksand, Overlock } from 'next/font/google';

export const quicksand = Quicksand({ subsets: ['latin'] });
export const overlock = Overlock({ 
    weight: ['400', '700','900'],
    subsets: ['latin'] });